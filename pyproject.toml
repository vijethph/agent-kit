[project]
name = "testapp"
version = "0.2.0"
description = "A trivial Python application"
requires-python = ">=3.11"
dependencies = ["mylib>=1.1.3,<2.0.0"]

[build-system]
requires = ["uv_build>=0.12.17,<0.13"]
build-backend = "uv_build"

[tool.uv.sources]
mylib = { git = "https://github.com/vijethph/playground.git", tag = "v1.1.3" }
